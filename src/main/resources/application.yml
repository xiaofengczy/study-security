server:
  port: 8888

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/study
  security:
    user:
      name: admin
      password: admin123

mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: com.study.security.dao.entity
